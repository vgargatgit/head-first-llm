name: Implement and validate COH-2.1

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/complete-coh-2-1.yml'

permissions:
  contents: write

jobs:
  implement-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add and validate the Chapter 1 positional scaffold
        run: |
          python scripts/complete-coh-2-1.py
          node scripts/check-book-consistency.mjs

      - name: Commit COH-2.1 and remove temporary automation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/chapter-01.md docs/inference-loop-scene-master-plan.md docs/stories.md
          git rm scripts/complete-coh-2-1.py .github/workflows/complete-coh-2-1.yml
          git commit -m 'Add Chapter 1 positional scaffold'
          git push
