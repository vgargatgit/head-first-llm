# Head First LLMs

Canonical versioned workspace for the illustrated Transformer tutorial.

## Layout

- `src/` editable Markdown
- `dist/` self-contained HTML chapters
- `assets/` original chapter graphics
- `styles/` shared styling

Open `dist/index.html` to browse the current chapters.
